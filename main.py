# main.py — FastAPI app, routes, serves frontend

from fastapi import FastAPI

app = FastAPI(title="Lease Intelligence Pipeline")

# async def upload_lease(file: UploadFile) -> LeaseResponse:
#     ...

# async def get_lease(lease_id: int) -> LeaseResponse:
#     ...

# async def list_leases() -> list[LeaseResponse]:
#     ...
